container_commands:

    # Install pip itself!
    01_install_pip:
        command: yum install pip -y

    # Install pip dependencies
    02_install_other:
        #command: pip install <put package names here>